students = [["Dharma", 100.2],
           ["Karma", 100.1],
           ["Ching", 100],
           ]

grades = [score for name, score in students]

# unique_grades = sorted(set(grades))
# second_lowest = unique_grades[1]

# names = [name for name, score in students if score == second_lowest]
# names.sort()

# for n in names:
#     print(n)